# 机械臂精对齐机制（仅KFS调用） 
这是用于在深度相机D435I完成粗对齐（使机器人机身移动到物体前）后下一步的对齐状态--精对齐，它的作用是通过机械臂末端执行器上的摄像头（通常是一个小型USB相机）来获取更精确的位置信息，以便机器人能够更准确地定位和操作目标物体。
## 1. 机械臂精对齐机制的工作原理
机械臂精对齐机制通常包括以下几个步骤：
1. **摄像头数据采集**：机械臂末端执行器上的摄像头会捕捉目标物体的图像数据。这些图像数据包含目标物体的位置信息和姿态信息。
2. **图像处理**：通过图像处理算法（滤色使得到KFS边缘，用于对齐），从摄像头捕捉到的图像中提取目标物体的位置信息和姿态信息。这些算法可以帮助识别目标物体的特征点，从而计算出物体的精确位置和姿态。
3. **位姿调整**：根据图像处理得到的位置信息和姿态信息，机械臂控制系统会计算出需要调整的位姿（位置和姿态），并通过机械臂的运动控制算法来调整机械臂的末端执行器，使其与目标物体进行精确对齐。
4. **反馈循环**：在调整过程中，机械臂会持续监测摄像头的图像数据，以确保对齐过程的准确性。如果发现对齐不准确，机械臂会继续调整，直到达到预期的对齐效果。
5. 在该精对齐启动前需要在启动时进行一次手动输入KFS的颜色（蓝色or红色）来进行对己方的KFS进行识别滤色和对齐。
6. 我需要让它加入状态机内：整个对齐的状态机为：粗对齐（D435I）（完成后下发抬起机械臂信号，机械臂抬起，然后接收下位机完成抬起机械臂动作组信号，关闭深度相机）-->精对齐（开启机械臂末端USB摄像头）-->完成对齐（关闭USB摄像头，下发抓取信号，准备抓取）
   
## 2. 机械臂结构，及运行 
我们的机械臂结构比较简单，主要由两个部分组成：1.抬升机构（负责机械臂的垂直运动）2.前平移机构（在垂直结构上，负责机械臂的水平伸出），吸盘（在末端横杆上，与摄像头相连（但位置可能会有点偏移））。在精对齐阶段，机械臂会根据摄像头捕捉到的图像数据进行底盘位置调整，以确保末端执行器（吸盘）能够准确地对齐目标物体，向底盘发出调整指令（往左或往右或向前或向后平移多少，并解算成vx,vy发给下位机））。 

运行：在粗对齐完成后，机械臂会抬升到一定高度，并向前平移伸出，伸到KFS方块上方，USB相机和吸盘一并旋转至朝下（抬起机械臂动作组），然后进入精对齐状态。此时，机械臂末端的USB摄像头会开始捕捉图像数据，并通过图像处理算法计算出目标物体的位置信息和姿态信息。根据这些信息，机械臂控制系统会调整底盘的位姿（由于不方便更改机械臂的位姿，所以直接调整底盘），使其与目标物体进行精确对齐。整个过程会持续监测摄像头的图像数据，以确保对齐的准确性，直到达到预期的对齐效果。

## 3. 信号：
1. 粗对齐完成后下发抬起机械臂信号（机械臂抬起伸前，然后接收下位机完成抬起机械臂动作组信号，关闭深度相机）-->精对齐（开启机械臂末端USB摄像头）-->完成对齐（关闭USB摄像头，下发抓取信号，准备抓取） 
2. 抬起机械臂的信号分为两个：1. 抬升1（若需要抓取比当前台阶高的台阶上的物体，则发出抬升1信号）；2. 抬升2（若需要抓取比当前台阶低的台阶上的物体，则发出抬升2信号）。例子：机器人在2号台阶需要抓取5号台阶上的物体，则发出抬升1信号；机器人在5号台阶需要抓取6号台阶上的物体，则发出抬升2信号。各编号台阶高度看ros2_ws/src/decision_processor/decision_processor/config.py  
   
不要修改我的triple_edge_align.py脚本的整体逻辑，和变量参数等等，只需要在其中加入机械臂精对齐机制的相关代码即可。请确保在加入代码时保持代码的清晰和可读性，并且不要影响原有功能的正常运行。